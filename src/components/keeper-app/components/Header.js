import React from 'react';

const Header = () => {
    return (
        <header className="header shadow bg-warning shadow p-3 mb-5">
            <div className="container">
                <h1 className="text-light font-monospace">Keeper App</h1>
            </div>
        </header>
    );
};

export default Header;